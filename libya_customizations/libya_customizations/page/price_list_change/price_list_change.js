frappe.pages['price-list-change'].on_page_load = function(wrapper) {
    // Create the page layout
    var page = frappe.ui.make_app_page({
        parent: wrapper,
        title: 'Price List Change',
        single_column: true
    });

    
};
